function lastIndexOf(str,str1){
var p ;
  for(var i = 0; i < str.length; i++){
    for(var m = 0; m < str1.length; m++){
      if(str[i + m] = stri[m]) {
        if(m == 3){
        p = i;
        }
      }
    }
  }
return p;
}
